# How to

1/ `uv run dbt init` with taxi_rides_ny to get a proper dbt project.
2/ add `.env` file and set `DBT_PROFILES_DIR` such that dbt redirects to a the project `profiles.yml` (I have another one)
3/ 